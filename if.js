/* Cubiculo de votaciòn */

var edad = 18;
if (edad === 18) {
    console.log("Felicitaciones es tu primera votaciòn");
} else if (edad > 18) {
    console.log("Es probable que esta sea tu segunda votaciòn");
} else {
    console.log("Muy posiblemente eres menor de edad, lo que no te permite votar");
}

/* Otra de las formas de generar condicionantes es una sola linea de codigo, las siguientes dos lineas pertenecen a la sintaxis 
y, a un ejercicio pràctico */

/* Es importante inicializar precisamente las condiciones por medio de un variable */


condition ? true : false

var real = 5

var comprobacion = real > 0 ? `${real} + es un numero positivo` : `${real} + es un numero negativo`;

