
// Los condicionales son basicamente una forma de que si un argumento es verdadero o falso, este a a rrojar un resultado o un none dpeendiende de lo que quiera el woftware witer


/* Aqui la estructura básica de un IF convencional */

let a = 1 ;

if ( a === 1 ) { //La primera validación //Se usan los tres iguales para decir que debe ser exactamente igual, (EXACTOOO!)
    console.log("a es igual a 1") 
} else if (a != 1) { // else if se usa para evaluar otra condición
    console.log("a es diferente que 1") 
} else { // se usa para la última validación
    console.log("a no es un numero") 
}

